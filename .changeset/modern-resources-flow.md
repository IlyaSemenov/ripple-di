---
"ripple-di": major
---

Use `dispose: true` to clean up dependency values through `Symbol.asyncDispose` or `Symbol.dispose`, and use `Scope`, `Installation`, and `Runtime` with `await using`.
Node.js 18.18 or newer is now required.
